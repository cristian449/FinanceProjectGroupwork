@model IEnumerable<FinanceProject.Models.User> 


@* Practically Serves Absolutely NO PURPOPOSSEESSBecause how the duckx do i see if a user is active if me use localdb, though it does show admin as inactive despite being logged in
    , will investiage later *@
<h2>Currently active Users</h2>
<table>
    <tr>
        <th>User ID</th>
        <th>FirstName</th>
		<th>LastName</th>
        <th>Email</th>
    </tr>
    @foreach (var user in Model.Where(i => i.IsActive))
    {
        <tr>
            <td>@user.Id</td>
            <td>@user.FirstName</td>
            <td>@user.LastName</td>
            <td>@user.Email</td>
            <td>
                <a asp-action="UserDetails" asp-route-id="@user.Id" class="action-btn btn btn-info">Details</a>
                <form asp-action="DeleteUser" asp-route-id="@user.Id" method="post" style="display:inline;">
                    <button type="submit" class="action-btn btn btn-danger" onclick="return confirm('Are you sure you want to delete this user?');">Delete</button>
                </form>
            </td>
        </tr>
    }
</table>

<h2>Currently Inactive Users</h2>
<table>
    <tr>
        <th>User ID</th>
        <th>FirstName</th>
		<th>LastName</th>
        <th>Email</th>
    </tr>
    @foreach (var user in Model.Where(i => !i.IsActive))
    {
        <tr>
            <td>@user.Id</td>
            <td>@user.FirstName</td>
            <td>@user.LastName</td>
            <td>@user.Email</td>
            <td>
                <a asp-action="UserDetails" asp-route-id="@user.Id" class="action-btn btn btn-info">Details</a>
                <form asp-action="DeleteUser" asp-route-id="@user.Id" method="post" style="display:inline;">
                    <button type="submit" class="action-btn btn btn-danger" onclick="return confirm('Are you sure you want to delete this user?');">Delete</button>
                </form>
            </td>
        </tr>
    }
</table>

